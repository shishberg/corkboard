# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.serena/
device/target/
device/data/
# Holds the device's runtime state incl. config.json with the secret iCal URL —
# never commit it (listed explicitly even though device/data/ already covers it).
device/data/config.json

# Playwright
test-results/
playwright-report/
.playwright-mcp/
